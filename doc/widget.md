## 通用组件

- `EngPage`：统一页面脚手架，提供标题、副标题、背景和 AppBar 插槽。
- `EngPageBackground`：统一页面纸张纹理背景，只负责视觉承载。
- `EngPanel`：无阴影、低圆角的信息面板，用于首页、统计、设置和管理页内容分组。
- `EngIconBadge`：图标徽章，用于指标、空状态和列表项的视觉锚点。
- `EngEmptyState`：统一空状态组件，支持图标、标题、说明和主操作按钮。
